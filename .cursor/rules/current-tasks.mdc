# ION DEX Frontend — Current Task Dispatch
## CO Round 32 | 2026-06-25 22:37 CST

---

## ⚠️ ESCALATION NOTICE
W3 (CE/RE/DTE) dispatched R29 08:41 — **14h elapsed, no completion signal.**
W4 (VQE) dispatched R30 08:46 — **14h elapsed, no completion signal.**
Re-dispatching with escalated priority. Adding W5 (BE/CRE/CG) in parallel.

---

## Active Dispatch: W3 + W4 + W5 (Parallel)

### Wave 3 — P1 UI Polish & Audit (RE-DISPATCHED 🔴)
| # | Role | Task | File(s) | Status |
|---|------|------|---------|--------|
| 7 | **CE** | Global Glass Morphism Polish | All 5 pages | 🔴 Escalated |
| 8 | **RE** | Responsive Verification | All pages | 🔴 Escalated |
| 9 | **DTE** | Design Token Audit | `src/lib/design-tokens.ts` | 🔴 Escalated |

**CE — Execute NOW:**
1. READ `src/lib/design-tokens.ts` — ALL visual values from DesignTokens
2. READ `src/app/swap/page.tsx`, `src/app/pool/page.tsx`, `src/app/stake/page.tsx`, `src/app/bridge/page.tsx`, `src/app/page.tsx`
3. Apply `effects.glassBase` + `effects.cardGlow` + `effects.backdropBlur` to all card containers
4. Apply `colors.neonCyan` glow to active elements, `colors.neonMagenta` to warnings
5. **Zero hardcoded hex/rgba** — ONLY DesignTokens imports
6. After changes: run `node scripts/visual-diff.mjs all` — target < 1% diff

**RE — Execute NOW:**
1. Test all 5 pages at breakpoints: 375px (mobile), 768px (tablet), 1440px (desktop)
2. DEXGridHarness must hold grid structure at all widths
3. Touch targets ≥ 44px on mobile buttons
4. Tab navigation must work with keyboard arrows
5. Run: `npx playwright test --viewport="iPhone 13"` and `--viewport="iPad Pro"` and `--viewport="Desktop Chrome"`

**DTE — Execute NOW:**
1. READ `src/lib/design-tokens.ts` completely
2. Cross-reference every token used in 5 page files vs tokens defined
3. Report missing/orphan tokens
4. Verify: `glassBase`, `neonCyan`, `neonMagenta`, `cardGlow`, `backdropBlur`, `textPrimary`, `textSecondary`, `borderRadius`, `panelGap`, `panelPadding` all exist
5. If tokens missing → add them (R3 operation: needs GSE or CG co-sign per permission matrix)

---

### Wave 4 — P2 Visual Diff QA (RE-DISPATCHED 🔴)
| # | Role | Task | File(s) | Status |
|---|------|------|---------|--------|
| 10 | **VQE** | Visual Diff QA Pass | All 5 pages | 🔴 Escalated |

**VQE — Execute NOW:**
1. Run `node scripts/visual-diff.mjs all`
2. For each page with pixel diff > 1%: identify deviating component
3. Fix ONLY via DesignTokens.ts values — NO hardcoded colors in components
4. Re-run diff until 5/5 pages < 1%
5. After all pass: verify DEXConsole tab navigation works
6. Output: pass/fail report per page with diff percentages

---

### Wave 5 — P2 Build + Review + Compliance (NEW DISPATCH 🟡)
| # | Role | Task | File(s) | Status |
|---|------|------|---------|--------|
| 11 | **BE** | Next.js Build Optimization | `next.config.ts`, `package.json` | 🟡 Dispatched |
| 12 | **CRE** | Code Review — W2 Output | All 5 pages | 🟡 Dispatched |
| 13 | **CG** | .cursorrules Compliance Audit | All pages + `.cursor/rules/` | 🟡 Dispatched |

**BE — Build Engineer:**
1. READ `next.config.ts` and `package.json`
2. Run `npx next build` — capture output, report errors/warnings
3. Check bundle size: run `npx @next/bundle-analyzer` if available
4. Verify Turbopack config is correct
5. Check for unused dependencies in `package.json`
6. Report: build success/fail, bundle size, optimization recommendations
7. **R2 permission on config/build** — can modify `next.config.ts` directly

**CRE — Code Review Engineer:**
1. READ all 5 page files: `src/app/page.tsx`, `swap/page.tsx`, `pool/page.tsx`, `stake/page.tsx`, `bridge/page.tsx`
2. READ `src/lib/design-tokens.ts` and `src/components/layout/DEXGridHarness.tsx`
3. Audit checklist per file:
   - [ ] Zero hardcoded hex/rgba colors (all from DesignTokens)
   - [ ] All pages wrapped in DEXGridHarness
   - [ ] No mock data (real contract addresses/RPC calls)
   - [ ] No CSS fixed/absolute positioning (Grid only)
   - [ ] 3D icons use `<img>` tags, not CSS/SVG
   - [ ] File ≤ 300 lines each
4. Output: per-file review report with pass/fail per checklist item
5. Gate decision: P0→P1 valve pass or fail

**CG — .cursorrules Guardian:**
1. READ `.cursorrules` or `.cursor/rules/*.mdc` — all rule files
2. Cross-check compliance across 5 pages:
   - Zero-Visual-Discretion: no hardcoded visual values
   - Grid-Only: CSS Grid, no margin/flex positioning
   - Asset-Img-Only: icons/images via `<img>` tag only
3. Scan for violations: `Select-String -Pattern "#[0-9a-fA-F]{6}|rgba?\(" -Path src/app/**/*.tsx`
4. Report: violations list with file:line references
5. If violations found → output remediation instructions per file
6. **R3 permission on .cursorrules** — can modify rules directly

---

## Completed Waves

### Wave 2 — P0 Core Pages (COMPLETE ✅)
| # | Role | Task | Lines | Status |
|---|------|------|:-----:|--------|
| 1 | UCA | Swap Component Architecture | 277 | ✅ |
| 2 | CE | Swap Visual Style | 277 | ✅ |
| 3 | LE | Pool Page Layout | 206 | ✅ |
| 4 | IE | Stake Interaction | 129 | ✅ |
| 5 | LE | Bridge Page Layout | 117 | ✅ |
| 6 | CE | Home Dashboard Polish | 132 | ✅ |

**Total: 861 lines across 5 pages.**

---

## Queue → Task Traceability

| Queue File | Wave | Tasks | Status |
|------------|------|-------|--------|
| `0001-p0-pages-task.md` | W2 | Pages created | ✅ Done |
| `p0-frontend-tasks.md` | W2 | All 6 tasks implemented | ✅ Done |
| `p0-visual-qa-pass.md` | W4 | VQE dispatched | 🔴 In Flight |
| `p1-ui-polish.md` | W3 | CE, RE, DTE dispatched | 🔴 In Flight |

### Result Archive (context-only, no pending tasks)

| Result File | Priority | Category | Role Match |
|-------------|----------|----------|------------|
| `001-func-compile-and-forge-build.md` | P0 | Contract compile | BE (backend scope) |
| `002-stress-test-100-rounds.md` | P0 | Contract testing | VQE (test scope) |
| `003-real-contracts-and-data.md` | P1 | Config/data layer | UCA + PE |
| `004-ion-only-fees.md` | P1 | Fee policy | UCA + BE |
| `005-ui-polish-responsive.md` | P2 | UI polish + responsive | CE + RE |
| `006-testnet-deploy-e2e.md` | P3 | Deploy + E2E | BE + VQE |
| `007-ion-dex-phase1-swap-pool.md` | P0-P2 | Phase 1 master | PM + UCA |
| `pool-card-task.md` | P0 | PoolCard component | UCA + CE |

---

## Roles Active: 7/18
**CE** | **RE** | **DTE** | **VQE** | **BE** | **CRE** | **CG**

## Roles Idle: 11/18
PM | DD | UCA | LE | AM | IE | AE | PE | i18n | GSE | CO

---

## Valve Status
- ✅ P0 Valve: All 11 roles + 4 system conditions green
- 🔴 P1 Valve: CE/RE/DTE in flight (14h overdue — escalated)
- 🔴 P2 Valve: VQE in flight (14h overdue — escalated); BE/CRE/CG dispatched
- 🔒 Release Valve: Blocked

---

## Next Gate
- W3 (CE/RE/DTE) + W4 (VQE) + W5 (BE/CRE/CG) complete
- → P1 Valve: CRE signs off on code quality
- → P2 Valve: VQE confirms visual diff < 1%
- → W6: PE (performance optimization) + AE (accessibility audit) + i18n (bilingual)
- → Release Valve: CRE + CG dual R3 sign → Production ready

---

## CO Round 32 Decision Log
- **R32-01:** W3/W4 14h elapsed without completion — escalating to 🔴, re-dispatching with detailed step-by-step instructions
- **R32-02:** No new queue files since R30 — queue frozen since 06-24 01:01
- **R32-03:** W5 pre-dispatched in parallel — BE (build), CRE (review), CG (compliance) are non-conflicting with W3/W4
- **R32-04:** CRE can audit W2 output (completed) independently of W3 (in-flight edits)
- **R32-05:** CG scans for hardcoded color violations — read-only audit, no source conflict
- **R32-06:** BE runs `next build` — captures current state, doesn't modify source pages
- **R32-07:** 7 active roles this round (up from 4 in R30)
