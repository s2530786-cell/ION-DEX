# Current Tasks — CO Dispatch 2026-06-24 17:07 CST

## P0 Tasks (Gate: 11 roles + 4 system conditions)

---

### [BE] Build Engineer — Page Routes & Build Config
**File:** `cursor-queue/0001-p0-pages-task.md`
**Keywords:** build(1), config(1), next(1), deploy(0), ci(0) → BE=3
**Permission:** R1→R2 (config/build)
**Steps:**
1. Create `src/pages/index.tsx` — Home page wrapping DEXConsole
2. Create `src/pages/swap.tsx` — Standalone swap page with SwapPanel + WalletHarness
3. Create `src/pages/pool.tsx` — Standalone pool page with PoolPanel + WalletHarness
4. Create `src/pages/stake.tsx` — Standalone stake page with StakePanel + WalletHarness
5. Create `src/pages/bridge.tsx` — Bridge placeholder
6. Create `next.config.ts` — Basic Next.js config
7. Run `npx next build` until zero errors
**Constraints:** ALL colors from design-tokens.ts, ALL pages in DEXGridHarness, NO hardcoded hex/rgba

---

### [VQE] Visual QA Engineer — Visual Diff QA Pass
**File:** `cursor-queue/p0-visual-qa-pass.md`
**Keywords:** test(1), visual(1), screenshot(1), diff(1), QA(1), baseline(1) → VQE=6
**Permission:** R1 (source/config/build/deploy)
**Steps:**
1. Run `node scripts/visual-diff.mjs` to compare current render vs baselines
2. For each page with pixel diff > 0%: identify deviating component
3. Fix ONLY via DesignTokens.ts values — NO hardcoded hex/rgba
4. Re-run diff until < 1% for all 5 pages
5. After all pass: verify DEXConsole page routes between 5 pages with tab navigation
**Constraints:** ALL colors from design-tokens.ts, ALL pages in DEXGridHarness, script at scripts/visual-diff.mjs

---

### [UCA] UI Component Architect — Swap Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 1)
**Keywords:** component(1), api(1), interface(1), swap(1), router(1) → UCA=5
**Permission:** R2 (source)
**Steps:**
1. Read `src/lib/design-tokens.ts` and `.cursorrules` FIRST
2. State "Layout Intent Analysis" before coding
3. Implement token select dropdown (ION/WBNB/USDT) via real PancakeSwap router
4. Implement amount input with "MAX" button
5. Implement price quote display (rate + slippage)
6. Implement swap button with confirmation flow
7. Wrap everything in DEXGridHarness, use DesignTokens only
8. After completion: run `npx playwright test` for visual regression
**Constraints:** ZERO hardcoded colors, Grid-Only layout, Asset-Img-Only, connect to real PancakeSwap router

---

### [UCA] UI Component Architect — Pool Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 2)
**Keywords:** component(1), api(1), interface(1), pool(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. Read `src/lib/design-tokens.ts` and `.cursorrules` FIRST
2. State "Layout Intent Analysis" before coding
3. Implement pool list with TVL/APR/Volume columns (real data from PancakeSwap subgraph)
4. Implement Add Liquidity form (token pair + amounts)
5. Implement Remove Liquidity form
6. Use existing PoolCard component from `src/components/DEX/PoolCard.tsx`
7. Wrap in DEXGridHarness, use DesignTokens only
8. After completion: run `npx playwright test` for visual regression
**Constraints:** ZERO mock data, real on-chain/subgraph data, all colors from design-tokens.ts

---

### [UCA] UI Component Architect — Stake Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 3)
**Keywords:** component(1), api(1), interface(1), stake(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. Read `src/lib/design-tokens.ts` and `.cursorrules` FIRST
2. State "Layout Intent Analysis" before coding
3. Implement Stake ION form (amount + duration selector)
4. Implement Unstake form
5. Implement Rewards display
6. Implement APR calculator
7. Use existing StakePanel component
8. Wrap in DEXGridHarness, use DesignTokens only
9. After completion: run `npx playwright test` for visual regression
**Constraints:** NO mock data, real contract connections, all colors from design-tokens.ts

---

### [UCA] UI Component Architect — Bridge Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 4)
**Keywords:** component(1), api(1), interface(1), bridge(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. Read `src/lib/design-tokens.ts` and `.cursorrules` FIRST
2. State "Layout Intent Analysis" before coding
3. Implement chain selector (BSC ↔ ION)
4. Implement token selector
5. Implement amount input
6. Implement bridge status tracker
7. Use real bridge contract addresses
8. Wrap in DEXGridHarness, use DesignTokens only
9. After completion: run `npx playwright test` for visual regression
**Constraints:** Real bridge contracts, NO mock data, all colors from design-tokens.ts

---

### [LE] Layout Engineer — Home/Dashboard Polish
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 5)
**Keywords:** layout(1), grid(1), dashboard(1→PM=1), responsive(0) → LE=2, PM=1
**Permission:** R2 (source)
**Steps:**
1. Read `src/lib/design-tokens.ts` and `.cursorrules` FIRST
2. State "Layout Intent Analysis" before coding
3. Implement ION price chart (DexScreener/GeckoTerminal data)
4. Implement TVL display
5. Implement 24h volume display
6. Implement Quick Action cards (Swap/Pool/Stake/Bridge)
7. Use CSS Grid layout, responsive breakpoints
8. Wrap in DEXGridHarness, use DesignTokens only
9. After completion: run `npx playwright test` for visual regression
**Constraints:** Real market data, Grid-Only layout, no hardcoded colors

---

## P1 Tasks (Gate: 7 roles check)

### [CE] CSS Engineer — UI Polish & Navigation
**File:** `cursor-queue/p1-ui-polish.md`
**Keywords:** glow(1→CE), neon(0), polish(1→CE/style), tab(0), navigation(0), animation(0) → CE=2
**Secondary roles:** IE (navigation/transition) → IE=1, GSE (grid layout) → GSE=1
**Permission:** R2 (source)
**Steps:**
1. READ constraint files FIRST: design-tokens.ts, DEXGridHarness.tsx, .cursor/rules/ui-harness-protocol.mdc
2. Polish index.tsx: Add tab nav bar (Swap|Pool|Stake|Bridge) with neon glow active indicator
3. Polish swap.tsx: Add token selector dropdowns, price impact display
4. Polish pool.tsx: Add LiquidityPanel with pair display
5. Polish stake.tsx: Add staking stats (APY, TVL, your stake)
6. Polish bridge.tsx: Add BSC↔ION chain selector with arrow animation
7. Create DEXConsole.tsx: Tab nav (cyan neon glow on active), renders selected page, localStorage persistence
8. Run `node scripts/visual-diff.mjs all` — all pages < 1% diff
**Constraints:** ALL colors from DesignTokens, ALL pages in DEXGridHarness, CSS Grid only, no margin-based positioning

---

## Execution Order

```
P0 → BE (Page Routes) → VQE (Visual Diff QA Pass)
    ↓
    UCA×4 (Swap→Pool→Stake→Bridge) + LE (Home Dashboard)
    ↓
P1 → CE (UI Polish & Navigation)
```

## Valve Status

| Valve | Status | Detail |
|-------|--------|--------|
| P0 Valve | 🔹 IN PROGRESS | BE×1 + VQE×1 + UCA×4 + LE×1 = 7 tasks pending |
| P1 Valve | 🔒 BLOCKED | Awaiting P0 Valve all-green |
| P2 Valve | 🔒 BLOCKED | Awaiting P1 Valve all-green |
| Release Valve | 🔒 BLOCKED | Awaiting P2 Valve all-green |

## Role Assignment Summary

| Role | Tasks | Permission | Priority |
|------|-------|------------|----------|
| BE | 1 (Page Routes) | R1→R2 config/build | P0 |
| VQE | 1 (Visual Diff QA) | R1 source/config/build/deploy | P0 |
| UCA | 4 (Swap/Pool/Stake/Bridge) | R2 source | P0 |
| LE | 1 (Home Dashboard) | R2 source | P0 |
| CE | 1 (UI Polish) | R2 source | P1 blocked |
