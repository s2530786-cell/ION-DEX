---
description: ION DEX 开发铁律 — Master 死命令，永不更改
alwaysApply: true
---

# ⚜️ ION DEX 开发铁律 — Master's Dead Law

> 旺财总经理传达，Master 钦定。永久有效，每次会话自动加载。
> 写入日期：2026-05-19 | 修订：14:05（补充全部攻击手段）

---

## 🔒 第一律 · 安全第一位 (Security First)

**写智能合约时必须通过 15 大类 Web3 攻击防御测试 + 抗量子攻击验证。1500 次全绿才能进入下一步。**

### 🛡️ 15 大类必测攻击（每类 100 次绿 → 1500 次底线）

| # | 攻击类型 | 测试内容 |
|---|---------|---------|
| 1 | **重入攻击** | 单函数重入、跨函数重入、跨合约重入、只读重入 |
| 2 | **闪电贷攻击** | 价格操控、预言机操控、流动性抽干、抵押率操纵 |
| 3 | **预言机操控** | 过期价格、TWAP 扭曲、低流动性喂价、单源依赖、闪电贷扭曲 |
| 4 | **三明治攻击 / MEV** | 前置交易、后置交易、JIT 流动性、三明治套利、套利机器人 |
| 5 | **权限绕过** | 未授权管理员、角色升级、tx.origin 误用、delegatecall 注入、无保护 selfdestruct |
| 6 | **整数溢出/下溢** | 所有算数运算、边界值、unchecked 块、舍入误差/精度损失 |
| 7 | **拒绝服务 (DoS)** | Gas 耗尽、区块填充、无界循环、外部调用失败、Block Gas Limit |
| 8 | **假币/粉尘攻击** | 伪造代币、无限授权、粉尘攻击、零值转账、假 ERC-20 |
| 9 | **时间戳操控** | block.timestamp 依赖、deadline 绕过、区块时间操纵 |
| 10 | **治理攻击** | 闪电贷投票、提案操控、时间锁绕过、投票权操纵 |
| 11 | **跨链桥攻击** | 消息验证绕过、验证者妥协、重放攻击、假事件 |
| 12 | **升级代理攻击** | 存储冲突、初始化漏洞、UUPS/透明代理、实现合约自毁 |
| 13 | **签名攻击** | 签名重放、签名可塑性、EIP-712 钓鱼、permit 钓鱼、无限制授权 |
| 14 | **逻辑漏洞** | 业务逻辑缺陷、未检查返回值、强制转账(ETH)、竞态条件/重排序 |
| 15 | **抗量子攻击** | 后量子签名方案、密钥长度、哈希函数抗量子、格密码验证 |

**铁律：1499 绿 + 1 红 = FAIL。回到审计步骤重来。**

### 📊 2024-2025 真实损失数据（警钟）
- 2024 重入攻击：$47M / 22 起
- 2024 预言机操控：$52M / 37 起
- 2024 MEV 收入：>$1.1B
- 2025 DeFi 总损失：>$1.7B（超过 2024 全年）
- 80%+ 被盗资金来自链下（私钥泄露/钓鱼）

---

## 🔄 第二律 · 开发循环 (Dev Cycle)

```
读 SESSION_STATE.md
  → 编译 (compile-func.mjs / forge build / npm run build)
  → 审计漏洞 (15 类攻击检查)
  → 修复 (逐一修复每个漏洞)
  → 重编译 (每修复一个就重编)
  → git commit (每次通过都要提交)
  → 1500 次安全攻击测试
  → 通不过回到「审计漏洞」重来
  → 全绿通过 → commit → push → 更新 SESSION_STATE.md
```

**从不跳步。从不假设通过。从不带着已知错误前进。**

---

## 🤖 第三律 · 自主审查 (Self-Review)

- **不需要人工确认。** 自己审查代码、自己找问题、自己修补漏洞。
- 每次代码改动后：自查 → 找问题 → 修复 → 重验证。
- 发现安全漏洞立即修复，不等人指出。
- 把每行代码当作 CertiK / Trail of Bits / OpenZeppelin / Halborn 审计对象。
- build 不通过不能 commit。测试不通过不能 commit。
- **Cursor 沙盒测试** — 利用 Cursor 内置沙盒在隔离环境测试所有攻击模拟。

---

## 🌐 第四律 · 24/7 不间断 (Continuous Operation)

- **永不闲置。不等任何人。不休息。**
- 完成 Phase N → 立即开始 Phase N+1。
- 遇到阻塞 → 记录到 SESSION_STATE.md → commit → 找下一个独立任务。
- 每 30 分钟重跑一次：编译 → 审计 → 修复 → commit 循环。
- 绝不输出 "waiting for user" — 找其他事做或重跑审计。

---

## 🧹 第五律 · 零垃圾 (Zero Garbage)

- **禁止：** `motionmotiondiv`、`bian'jibianji`、`hi`、`test`、拼音、占位符。
- **禁止：** 连续 3 行以上空行。
- **禁止：** 未闭合的 JSX 标签、未导入的组件。
- **禁止：** 破坏 UTF-8 编码（所有文件 UTF-8 无 BOM）。
- **每行代码必须是有效语法。** 无效语法 = 垃圾。

---

## 🔧 第六律 · 实干验证 (Verify Everything)

- **自己验证效果，自己查找问题，不让 Master 去看。**
- **没有工具就去 GitHub 搜、下载、安装、集成。**
- **下载/安装不算完成。所有依赖装全，服务真正跑起来。**
- `npm run build` 必须零错误通过。
- `scripts/compile-func.mjs` 必须 22/22 绿。
- 每个 commit 必须 push 到 `origin 2026-05-19-q7fx`。

---

## ⚡ 第七律 · 准时汇报 (Report on Time)

- **每小时：** 汇报资产全景到 Telegram。
- **每次完成 Phase：** git commit + push + 更新 SESSION_STATE.md + 通知旺财。
- **每次失败：** 记录失败原因 + 修复方案 + 重试次数。

---

_此铁律由 Master 钦定，旺财总经理传达。永不更改，永不削弱。刻进基因。_
