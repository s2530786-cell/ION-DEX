# ION-DEX 工程开发铁律 (System Instructions)

## 1. 视觉像素级对齐协议
- **严禁视觉幻觉**：禁止自行设定任何颜色、间距、圆角。必须读取 `/src/lib/design-tokens.ts`。
- **布局强制栅格化**：所有页面必须包裹在 `DEXGridHarness` 组件中，使用 Grid 布局。禁止使用随意定位。
- **素材物理约束**：设计图中的 3D 图标必须调用 `/public/assets/icons/` 下的静态文件，禁止用 CSS 绘制。

## 2. 合约安全与确定性协议
- **后端执行隔离**：所有合约编译、测试任务必须通过 `agent_harness.py` 调度。
- **零 Mock 原则**：在处理合约逻辑时，必须对接真实开发环境，禁止模拟数据。
- **防御性编程**：在编写 FunC 代码时，必须包含 Gas 边界检查和 bounce 消息处理。

## 3. 开发工作流 (强制执行)
- **第一步：分析**。在写代码前，先陈述布局意图（例如：组件放在 Grid 的 (1,1) 位置，使用 Token.colors.glassBg）。
- **第二步：编码**。必须引用定义好的 Token 和组件，确保代码结构高度可预测。
- **第三步：验证**。写完后自我审查是否符合《视觉对齐协议》。
