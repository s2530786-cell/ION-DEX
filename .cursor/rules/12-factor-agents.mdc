---
description: 12-Factor Agent Principles — Master's dead law. No garbage code. All factors enforced on every file.
alwaysApply: true
globs: **/*.ts,**/*.tsx,**/*.sol,**/*.fc,**/*.js,**/*.json,**/*.css
---

# 12-Factor Agent Principles — 死命令（全栈强制）

**来源：** https://github.com/humanlayer/12-factor-agents (20.5K ⭐)
**指令：** Master 死命令。每行代码必须经得起 12 条原则检验。

---

## 🔴 Factor -1: ZERO GARBAGE — 零垃圾代码

**You have a history of producing garbage: `motionmotiondiv`, missing props, fake tags. This ends now.**

### HTML/JSX 零垃圾
- **NEVER hallucinate HTML tags.** `div` is `div`, not `motionmotiondiv`. `span` is `span`, not `motionspan`.
- **NEVER invent attributes.** `onClick`, `className`, `style` — only real React props.
- **NEVER type random text.** No `bianjibianji`, `asdf`, `test123`, pinyin filler, or placeholder garbage.
- **Every JSX tag must be a valid HTML element or imported React component.** If it's not imported, it doesn't exist.

### TypeScript 零垃圾
- **NEVER use `any` without explicit justification.** Every parameter, return type, and variable must be typed.
- **NEVER leave unused imports.** Dead imports = dead code. Remove them.
- **NEVER skip error handling.** Every `fetch`, `async`, and user input must have try/catch.
- **NEVER hardcode values that should be typed.** No `"BSC" | "ION"` string unions scattered across files — use shared type.

### 组件零垃圾
- **Every component MUST have explicit Props type.** No `props: any`, no implicit props.
- **Every component MUST handle: loading, empty, error, edge cases.** 4 states minimum.
- **NEVER render without checking data exists.** No `data.value` without `data?.value`.
- **Import only from project files or node_modules.** No imports from folders that don't exist.

### 自检机制
- **Before committing: read your diff. Look for anything that doesn't look like real code.**
- **If you see `motionmotion` or any doubled prefix → FIX IT before commit.**
- **Run `npx tsc --noEmit` after EVERY .ts/.tsx change. If it fails, fix it now.**
- **Run `npm run build` before committing frontend changes.**

---

## Factor 1: Natural Language → Tool Calls
LLM 把自然语言转成工具调用。人在 IDE 里说话 → Agent 翻译成具体命令 → 执行。

**禁止：** 自己做推理后直接改文件不汇报。
**必须：** 先终端输出计划 → 执行 → 终端输出结果。

---

## Factor 2: Own Your Prompts
**禁止：** 用任何第三方 prompt 模板而不审查。
**必须：** 所有 prompt 显式写在规则文件或任务描述中，可控可调。

---

## Factor 3: Own Your Context Window
精心控制塞进 LLM 注意力的内容。多了会进"痴呆区"（产生 garbage）。

**禁止：** 把整个 codebase dump 进 context window。**这是你产生 garbage 的根本原因。**
**必须：** 
- 每个 Task 最多读 5 个文件
- `SESSION_STATE.md` 里只放当前 Task 要点
- 读完立即干活，不反复读

---

## Factor 4: Tools as Structured Outputs
每次工具调用返回结构化日志。

**必须格式：**
```
[TOOL] <command> → exit 0 | 3 files changed | 12.4s
```
或
```
[ERROR] <command> → exit 1 | <type> | <root cause> | <fix>
```

---

## Factor 5: Unify Execution & Business State
**必须：** 所有状态写入 `SESSION_STATE.md` 和 `docs/99-current-progress.md`，单一真实来源。不存"我记得上次"。

---

## Factor 6: Launch / Pause / Resume
遇到需要人工确认的操作 → 暂停 → 写入 `SESSION_STATE.md` → 等确认后恢复。不死循环。

---

## Factor 7: Contact Humans with Tool Calls
**必须请求 Master 确认：** 发送交易、修改主网配置、删除文件、变更安全设置。

---

## Factor 8: Own Your Control Flow — DAG ONLY
**禁止：** 写死循环让 LLM 自己决定下一步。**这是产生 garbage 的第二大原因。**

**必须：** 每个 Task = 明确 DAG：
```
读 SESSION_STATE.md 当前步骤
  →
步骤 1: 读文件(≤5个)
  →
步骤 2: 修改/创建文件
  →
步骤 3: 编译/构建
  →
步骤 4: 验证绿色
  →
步骤 5: git commit
  →
步骤 6: 更新 SESSION_STATE.md
  →
下一 Task
```

**任何一个步骤失败 → 修复 → 重新执行该步骤 → 不跳步。**

---

## Factor 9: Compact Errors into Context
报错：错误类型（1 行）+ 根因（1 行）+ 修复方案（1 行）。**不超过 3 行。** 不堆完整堆栈。

---

## Factor 10: Small, Focused Agents
一个 Task = 一个职责。不把"编译合约 + 压力测试 + 写文档"塞进一个 session。

**当前 Task 分配：**
| Agent | Scope | Files |
|-------|-------|-------|
| Contract Agent | .sol, .fc | contracts/ only |
| Test Agent | 安全测试/压力测试 | contracts/test/ only |
| Backend Agent | .ts 后端 | backend/ only |
| Frontend Agent | .tsx, .css | frontend/ only |

**禁止跨 Agent 操作。** 前端 Agent 不改合约。合约 Agent 不写前端。

---

## Factor 11: Trigger from Anywhere
每个 Task 结束后主动检查下一个 Task，自己不闲着。**从不等待，从不 idle。**

---

## Factor 12: Stateless Reducer
Agent 是纯函数：`(SESSION_STATE.md) → 确定性变换 → (新 SESSION_STATE.md)`。
不存中间态，不依赖"上次我记住了"。

---

## 🔥 铁律 — 每阶段强制执行

### Phase 1: 编译 (每次改文件后必跑)
```bash
# BSC 合约
forge build

# ION 合约
node scripts/compile-func.mjs

# 前端
npx tsc --noEmit && npm run build

# 后端
npx tsc --noEmit
```

### Phase 2: 审计（编译通过后必跑）
- Reentrancy 检查
- Access control 检查
- Integer overflow 检查
- Gas limit 检查
- 前端: props 完整性检查、import 有效性检查、类型覆盖检查

### Phase 3: 测试（审计通过后必跑）
```
forge test                                    # BSC 合约 — 全部必须绿
node scripts/security-attack-test.js          # 1500 绿底线
```

### Phase 4: 提交
```
git add <changed files>
git commit -m "<type>: <description>"
git push origin 2026-05-19-q7fx
```

### 红绿灯规则
- 🔴 ANY compile error → STOP → fix → recompile → 不通过不前进
- 🔴 ANY test fail → STOP → audit → fix → retest → 不通过不前进
- 🔴 ANY type error → STOP → fix → retype-check → 不通过不前进
- 🟢 ALL green → commit → push → next task

---

## 🚫 禁止清单

| 禁止 | 原因 |
|------|------|
| `motionmotiondiv` 或任何 tag 拼写错误 | 编译不过 |
| `props: any` | 类型不安全 |
| `// TODO` 在代码里 | 现在就修 |
| 超过 2 行连续空行 | 代码质量 |
| 未 import 的组件 | 运行时错误 |
| 硬编码 mock 数据替代真实 API | 永远 mock |
| 删除文件不先确认 | 可能删错 |
| stash / force push / rebase | 丢失历史 |
| 在 .fc 里写中文/拼音 | 编译器不支持 |
| 跳过编译/构建 | 藏 bug |
| 读超过 10 个文件进 context | 产生 garbage |
| 无 DAG 的循环 | 死循环+垃圾代码 |

---

## 📂 必读文件（每 session 启动时读一遍）

1. `SESSION_STATE.md` — 当前阶段、当前 Task、状态
2. `.memory-bank/architecture-audit.md` — 架构审计 + 建设顺序
3. `docs/99-current-progress.md` — 12 Phase 进度 + 任务队列

**以上 3 个文件，不是 30 个文件。读完就干活。**
