# 🔴 P0-TASK: 前端代码提交 + 测试 + 队列消化

**旺财派发 | 2026-06-20 11:50 CST | 立即执行**

## 当前状态（旺财已验证）
- ✅ 5个核心页面完整实现：SwapPage(16KB) / PoolPage(12KB) / StakePage(7.6KB) / BridgePage(18KB) / DashboardPage(15KB)
- ✅ AiMarketPage(12KB) + AiStrategyConfig(6KB) + aiStrategy routes/service
- ✅ TSC 零错误
- ✅ Dev Server :3000 HTTP 200
- ✅ Pipeline 全绿
- ✅ 22个 Solidity 合约 + Forge 36/36 + verify-100 3600次全绿
- ✅ 后端 8个 API 路由 + 30+ services

## P0 任务（按顺序执行）

### 任务 1: 提交所有未提交代码
```
git add -A
git commit -m "feat: Phase 2 AI Market + frontend polish + design-tokens sync"
git push
```
**验证**: `git status` 显示 clean

### 任务 2: 消化 cursor-queue 里的 4 个任务
读取并执行以下文件中的任务：
- `cursor-queue/p0-frontend-tasks.md`
- `cursor-queue/p0-visual-qa-pass.md`
- `cursor-queue/p1-ui-polish.md`
- `cursor-queue/0001-p0-pages-task.md`

### 任务 3: 前端测试覆盖
- 添加 `frontend/package.json` 的 test 脚本（vitest）
- 为核心页面写至少 5 个单元测试
- 运行 `npm test` 确保全绿

## 铁律（每次操作前必读）
1. 零 mock 数据，全部对接真实 API
2. 文件 ≤300 行
3. 颜色从 design-tokens.ts 引用，禁止硬编码 hex/rgba
4. 写完立即 tsc 检查
5. commit 前跑 pipeline
6. 禁止中文乱码，UTF-8 无 BOM

## 验证命令
```
npx tsc --noEmit
node scripts/dev-preflight.mjs
powershell -File scripts/pipeline/pipeline-frontend.ps1 -Mode full
```
