# Current Tasks — CO Dispatch 2026-06-24 14:26 CST

## P0 Tasks (Gate: 11 roles + 4 system conditions)

---

### [BE] Build Engineer — Page Routes & Build Config
**File:** `cursor-queue/0001-p0-pages-task.md`
**Keywords:** build(1), config(1), next(1), ci(1), deploy(1) → BE=5
**Permission:** R1→R2 (config/build)
**Steps:**
1. Create `src/pages/index.tsx` — Home page wrapping DEXConsole
2. Create `src/pages/swap.tsx` — Standalone swap page with SwapPanel + WalletHarness
3. Create `src/pages/pool.tsx` — Standalone pool page with PoolPanel + WalletHarness
4. Create `src/pages/stake.tsx` — Standalone stake page with StakePanel + WalletHarness
5. Create `src/pages/bridge.tsx` — Bridge placeholder
6. Create `next.config.ts` — Basic Next.js config
7. Run `npx next build` until zero errors
**Constraints:** ALL colors from design-tokens.ts, ALL pages in DEXGridHarness, NO hardcoded hex/rgba

---

### [UCA] UI Component Architect — Swap Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 1)
**Keywords:** component(1), api(1), interface(1), swap(1), router(1) → UCA=5
**Permission:** R2 (source)
**Steps:**
1. READ `src/lib/design-tokens.ts` first — all colors/spacing from DesignTokens
2. READ `.cursorrules` — Zero-Visual-Discretion, Grid-Only, Asset-Img-Only
3. Implement `src/app/swap/page.tsx`:
   - Token select dropdown (ION/WBNB/USDT)
   - Amount input with "MAX" button
   - Price quote display (rate + slippage)
   - Swap button with confirmation
4. Connect to PancakeSwap router (real contract, not mock)
5. Wrap in DEXGridHarness
6. State "Layout Intent Analysis" before outputting code
7. After completion: run `npx playwright test` for visual regression
**Constraints:** No mock data, no hardcoded colors, real contract only

---

### [UCA] UI Component Architect — Pool Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 2)
**Keywords:** component(1), api(1), interface(1), pool(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. READ `src/lib/design-tokens.ts` first
2. Implement `src/app/pool/page.tsx`:
   - Pool list with TVL/APR/Volume columns
   - Add Liquidity form (token pair + amounts)
   - Remove Liquidity form
3. Use PoolCard component from `src/components/DEX/PoolCard.tsx`
4. Real data from PancakeSwap subgraph/on-chain
5. Wrap in DEXGridHarness
6. State "Layout Intent Analysis" before outputting code
**Constraints:** No mock data, no hardcoded colors

---

### [UCA] UI Component Architect — Stake Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 3)
**Keywords:** component(1), api(1), interface(1), stake(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. READ `src/lib/design-tokens.ts` first
2. Implement `src/app/stake/page.tsx`:
   - Stake ION form (amount + duration selector)
   - Unstake form
   - Rewards display
   - APR calculator
3. Use StakePanel component
4. Wrap in DEXGridHarness
5. State "Layout Intent Analysis" before outputting code
**Constraints:** No mock data, no hardcoded colors

---

### [UCA] UI Component Architect — Bridge Page Full Implementation
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 4)
**Keywords:** component(1), api(1), interface(1), bridge(1) → UCA=4
**Permission:** R2 (source)
**Steps:**
1. READ `src/lib/design-tokens.ts` first
2. Implement `src/app/bridge/page.tsx`:
   - Chain selector (BSC ↔ ION)
   - Token selector
   - Amount input
   - Bridge status tracker
3. Use real bridge contract addresses
4. Wrap in DEXGridHarness
5. State "Layout Intent Analysis" before outputting code
**Constraints:** No mock data, no hardcoded colors

---

### [LE] Layout Engineer — Home/Dashboard Polish
**File:** `cursor-queue/p0-frontend-tasks.md` (Task 5)
**Keywords:** layout(1), grid(1), dashboard(1) → LE=3
**Permission:** R2 (source)
**Steps:**
1. READ `src/lib/design-tokens.ts` first
2. Implement `src/app/page.tsx`:
   - ION price chart (from DexScreener/GeckoTerminal)
   - TVL display
   - 24h volume
   - Quick action cards (Swap/Pool/Stake/Bridge)
3. Use DEXGridHarness for layout
4. State "Layout Intent Analysis" before outputting code
**Constraints:** CSS Grid only, no margin-based positioning, DesignTokens only

---

### [VQE] Visual QA Engineer — Visual Diff QA Pass (All 5 Pages)
**File:** `cursor-queue/p0-visual-qa-pass.md`
**Keywords:** test(1), visual(1), screenshot(1), diff(1), QA(1), baseline(1) → VQE=6
**Permission:** R1 (source/config/build/deploy)
**Steps:**
1. Run `node scripts/visual-diff.mjs` to compare current render vs baselines
2. For each page with pixel diff > 0%:
   - Identify which component caused the deviation
   - Fix the component (ONLY modify DesignTokens.ts values — NO hardcoded colors)
   - Re-run diff until < 1%
3. After all 5 pass: create a DEXConsole page that routes between the 5 pages with tab navigation
**Constraints:** ALL colors from design-tokens.ts, ALL pages in DEXGridHarness, target 5/5 pages < 1% diff

---

## P1 Tasks (Gate: 7 roles — BLOCKED awaiting P0 all-green)

---

### [CE] CSS Engineer — UI Polish & Navigation (Phase 1)
**File:** `cursor-queue/p1-ui-polish.md`
**Keywords:** css(1), style(1), glow(1), glass(1), polish(1) → CE=5
**Permission:** R2 (source)
**Steps:**
1. READ constraint files: design-tokens.ts, DEXGridHarness.tsx, ui-harness-protocol.mdc
2. Polish 5 pages (src/pages/):
   - index.tsx: tab nav bar (Swap|Pool|Stake|Bridge) with neon glow active indicator
   - swap.tsx: token selector dropdowns, price impact display
   - pool.tsx: LiquidityPanel with pair display
   - stake.tsx: staking stats (APY, TVL, your stake)
   - bridge.tsx: BSC↔ION chain selector with arrow animation
3. Create DEXConsole.tsx: tab navigation + localStorage persistence
4. Run `node scripts/visual-diff.mjs all` — all pages must pass < 1% diff
**Constraints:** ZERO hardcoded hex/rgba, CSS Grid only, DesignTokens only

---

## Execution Order

```
P0-1 [BE]    → Page Routes & Build Config
      ↓
P0-7 [VQE]   → Visual Diff QA Pass (baseline verification)
      ↓
P0-2 [UCA]   → Swap Page (can parallel with P0-3~P0-6)
P0-3 [UCA]   → Pool Page
P0-4 [UCA]   → Stake Page
P0-5 [UCA]   → Bridge Page
P0-6 [LE]    → Home/Dashboard
      ↓
P1-1 [CE]    → UI Polish & Navigation (BLOCKED until P0 all-green)
```

## Valve Status

| Valve | Status | Detail |
|-------|--------|--------|
| P0 Valve | 🔹 IN PROGRESS | BE×1 + UCA×4 + LE×1 + VQE×1 pending execution |
| P1 Valve | 🔒 BLOCKED | Awaiting P0 Valve all-green |
| P2 Valve | 🔒 BLOCKED | Awaiting P1 Valve all-green |
| Release Valve | 🔒 BLOCKED | Awaiting P2 Valve all-green |

## Role Assignment Summary

| Role | Tasks | Permission | Active |
|------|-------|------------|--------|
| BE | 1 (P0-1) | R1→R2 config/build | ✔ |
| UCA | 4 (P0-2~P0-5) | R2 source | ✔ |
| LE | 1 (P0-6) | R2 source | ✔ |
| VQE | 1 (P0-7) | R1 source/config/build/deploy | ✔ |
| CE | 1 (P1-1) | R2 source | 🔒 P1 blocked |
